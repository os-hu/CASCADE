This directory contains resources that might be needed by a specific class. 
Usually, artifacts that are not written in python directly. 
(For example the extractors or modifiers for specific programming languages).

Right now, it contains the Java Extractor Jar file, which is used by the JavaExtractor class to extract code from Java files.
and to verify if a piece of java code is syntactically correct.